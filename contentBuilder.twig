{# Create a contentBuilder Matrix field, blocks with key and body subfields #}

{% set content = {} %}
{% for row in entry.contentBuilder.all() %}
    {% set content = content|merge({ (row.key) : row }) %}
{% endfor %}

{{ content.foo.body }}
